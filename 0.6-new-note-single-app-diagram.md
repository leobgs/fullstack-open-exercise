```mermaid
sequenceDiagram
    participant browser
    participant server

    Note over browser: User create note and click 'Save'
    
    Note right of browser: JavaScript catch input and <br/> add note into local DOM

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note right of server: Server save JSON into databases
    server-->>browser: HTTP 201 Created (JSON: {"message":"note created"})
    deactivate server

    Note right of browser: Browser (with JS) stay on same page<br/>without reload
